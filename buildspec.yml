version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - cd dist/hr-automation
      - zip -r ../../build.zip .
artifacts:
  files:
    - build.zip
